name: powerpixels
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - ipython
  - pip:
    - "spikeinterface[full,widgets]"
    - "docker"
    - "ibllib"
    - "ipympl"
    - "jupyter"
    - "spikeinterface-gui"
    - "huggingface-hub"
    - "skops"
    
